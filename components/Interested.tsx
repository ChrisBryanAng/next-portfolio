import PrimaryButtonClean from "./PrimaryButtonClean";

const Interested = () => {
  return (
    <div className="flex flex-col gap-4 justify-center items-center font-bold mt-28">
      <p className="font-Ibarra text-[50px] p-4 leading-tight text-center">
        Interested in doing a project together?
      </p>
      <PrimaryButtonClean label="Contact me" />
    </div>
  );
};

export default Interested;
